


REM Nmap Public Source License(based on GPLv2 but prohibid commercial embeded use cases)
choco install nmap

choco install nssm

choco install wireshark

choco install soapui

REM Papercut is a simplified SMTP server designed to only receive messages (not to send them on) with a GUI on top of it allowing you to see the messages it receives.
choco install papercut

REM GPLv2. Java. Angry IP Scanner (or simply ipscan) is an open-source and cross-platform network scanner designed to be fast and simple to use. It scans IP addresses and ports as well as has many other features.
choco install angryip

choco install -y portqry
